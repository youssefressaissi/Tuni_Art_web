<form method="post" action="{{ path('app_order_delete', {'orderId': order.orderId}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ order.orderId) }}">
    <button class="btn">Delete</button>
</form>
