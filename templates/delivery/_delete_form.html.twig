<form method="post" action="{{ path('app_delivery_delete', {'deliveryId': delivery.deliveryId}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ delivery.deliveryId) }}">
    <button class="btn">Delete</button>
</form>
