<form method="post" action="{{ path('app_gallery_delete', {'galleryId': gallery.galleryId}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ gallery.galleryId) }}">
    <button class="btn btn-danger">Delete</button>
</form>
