<form method="post" action="{{ path('app_delivery_agency_delete', {'agencyId': delivery_agency.agencyId}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ delivery_agency.agencyId) }}">
    <button class="btn">Delete</button>
</form>
