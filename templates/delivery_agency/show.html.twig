{% extends 'base.html.twig' %}

{% block title %}DeliveryAgency{% endblock %}

{% block body %}
    <h1>DeliveryAgency</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>AgencyId</th>
                <td>{{ delivery_agency.agencyId }}</td>
            </tr>
            <tr>
                <th>AgencyName</th>
                <td>{{ delivery_agency.agencyName }}</td>
            </tr>
            <tr>
                <th>AgencyAddress</th>
                <td>{{ delivery_agency.agencyAddress }}</td>
            </tr>
            <tr>
                <th>NbDeliveries</th>
                <td>{{ delivery_agency.nbDeliveries }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_delivery_agency_index') }}">back to list</a>

    <a href="{{ path('app_delivery_agency_edit', {'agencyId': delivery_agency.agencyId}) }}">edit</a>

    {{ include('delivery_agency/_delete_form.html.twig') }}
{% endblock %}
