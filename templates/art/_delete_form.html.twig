<form method="post" action="{{ path('app_art_delete', {'artRef': art.artRef}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ art.artRef) }}">
    <button class="btn">Delete</button>
</form>
