<form method="post" action="{{ path('app_auction_delete', {'auctionRef': auction.auctionRef}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ auction.auctionRef) }}">
    <button class="btn">Delete</button>
</form>
